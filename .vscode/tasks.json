{
	"version": "0.2.0",
	"configurations": [
	  {
		"name": "Debug (GDB)",
		"type": "cppdbg",
		"request": "launch",
		"program": "${workspaceFolder}/a.out",  // Caminho para o seu executável
		"args": [],
		"stopAtEntry": false,
		"cwd": "${workspaceFolder}",
		"environment": [],
		"externalConsole": true,
		"MIMode": "gdb",
		"miDebuggerPath": "/usr/bin/gdb",  // Caminho do gdb no seu sistema
		"preLaunchTask": "build",  // Se você tiver uma tarefa de build configurada
		"internalConsoleOptions": "openOnSessionStart"
	  }
	]
  }
  